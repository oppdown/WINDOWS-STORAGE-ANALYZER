[package]
name = "storage-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Explainable filesystem scan model for WINDOWS STORAGE ANALYZER"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
