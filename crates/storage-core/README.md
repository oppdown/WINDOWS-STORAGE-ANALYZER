# storage-core

The correctness-first scan model and recursive fallback traversal. It returns explainable JSON with logical byte totals, child counts, timestamps, and visible error records. Windows NTFS/MFT acceleration will implement the same contract rather than changing the UI’s semantics.

Run `cargo test -p storage-core` and `cargo run -p storage-core --bin storage-analyzer -- C:\\Users`.
