[package]
name = "windows-storage-analyzer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Tauri desktop bridge for WINDOWS STORAGE ANALYZER"
build = "build.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde_json = "1"
storage-core = { path = "../../../crates/storage-core" }
tauri = { version = "2", features = [] }

[lints]
workspace = true
