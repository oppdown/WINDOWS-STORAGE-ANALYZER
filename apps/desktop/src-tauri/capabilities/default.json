{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the local-first desktop shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
